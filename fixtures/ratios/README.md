# Ratio / derived-metric fixtures

**Status:** OQ-001 decision candidates.

- `golden_metrics.v0.1.json` fixes numeric formula compatibility, including one external Rustler D/L compatibility case.
- `status_cases.v0.2.json` provides executable input/status semantics for missing, unresolved, invalid, nonstandard and non-applicable cases.
- `schema_contract_example.v0.1.json` validates the BoatDesign v0.4 / ResolvedConfiguration v0.2 migration shape.

They become accepted conformance fixtures only when OQ-001 is explicitly accepted.
