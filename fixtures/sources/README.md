# Source Rights Fixtures

These fixtures exercise the OQ-007 Source Rights contract.

They are contract examples, not legal conclusions about arbitrary third-party sites.

`source_rights_cases.v0.1.json` contains representative source classes. Each embedded `source` object MUST validate against the accepted `specs/SOURCE_SCHEMA.v0.2.json`.
