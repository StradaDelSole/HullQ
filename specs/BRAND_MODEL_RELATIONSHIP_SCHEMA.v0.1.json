{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hullq.local/schemas/brand-model-relationship/0.1",
  "title": "HullQ Brand-BoatModel Relationship v0.1",
  "description": "Standalone independently identifiable relationship between a Brand and a BoatModel. Composes from relationship-applicability/0.1 for shared applicability fields. Supports multiple historical associations and optional applicability boundaries.",
  "allOf": [{"$ref": "https://hullq.local/schemas/relationship-applicability/0.1"}],
  "type": "object",
  "unevaluatedProperties": false,
  "required": ["id", "brand_id", "boat_model_id"],
  "properties": {
    "brand_id": {
      "type": "string",
      "minLength": 1,
      "description": "Reference to the Brand entity ID."
    },
    "boat_model_id": {
      "type": "string",
      "minLength": 1,
      "description": "Reference to the BoatModel entity ID."
    }
  }
}
