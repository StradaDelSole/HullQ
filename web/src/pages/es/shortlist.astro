---
// SLICE-0058: Spanish locale-prefixed anonymous local shortlist surface. See
// ../en/shortlist.astro for the shared rationale.
export const prerender = false;
import ShortlistPageBody from "../../components/ShortlistPageBody.astro";

const locale = "es" as const;

Astro.response.headers.set("X-Robots-Tag", "noindex");
Astro.response.headers.set("Cache-Control", "private, no-store");
---

<ShortlistPageBody locale={locale} />
